import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const onColor = Color(0xFF445566);

  static const background = Color(0xFFf4f4f6);

  static const mainColor = Color(0xFF000033);

  static const listTitle = Colors.white12;
}
